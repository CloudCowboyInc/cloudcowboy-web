# Cloud Cowboy — frontend environment
# Copy to .env.local for dev, and set the same in your CI/host (S3/CloudFront build).
# These two enable Supabase auth + CRM persistence. Until set, the app runs in
# Phase-1 local mode (no auth, local seed data).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
